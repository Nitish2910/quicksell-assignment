import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './MainApp';

const CreateRoot = ReactDOM.createRoot(document.getElementById('rootElement'));
CreateRoot.render(
    <App />
);

